version: '3.8'

services:
  katago-api:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    # Uncomment below to enable GPU support (requires nvidia-container-toolkit)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
